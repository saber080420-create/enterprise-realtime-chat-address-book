// 功能开关配置（前端）
// 函数级注释：
// - 本文件用于集中管理前端特性开关，支持按需启用/禁用各功能，以便灰度或临时硬隔离
// - 约定所有开关均为只读常量，供业务代码直接 import 使用
// - 如需动态切换，可后续接入后端配置拉取或本地存储，但当前按静态常量管理

/**
 * 是否启用“历史消息”功能
 * 函数级注释：
 * - 关闭(false)时：
 *   1) Chat.vue 中“查看历史消息”入口与加载提示被隐藏
 *   2) 任何解锁/滚动加载历史的逻辑都会短路，不触发 API
 *   3) 展示层仅保留“离线注入 + 本次登录后新消息”的过滤
 * - 开启(true)时：恢复原有历史消息逻辑
 */
export const HISTORY_ENABLED = true

// 预留：其他前端特性开关可在此处扩展
// export const SOME_FEATURE_ENABLED = true